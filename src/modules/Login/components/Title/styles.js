import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
	title: {
		flex: 1,
		maxHeight: 1000
	},
	titleText: {
		fontSize: 40,
		fontWeight: '800',
		color: 'white'
	}
})

export default styles;